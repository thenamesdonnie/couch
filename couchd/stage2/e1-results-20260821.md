# E0 + E1-functional results — 21 Aug 2026, ~22:52-23:05

Operator: Claude (Donnie present, initiated "do it now" after the sudo
sitting the same evening). All runs on the fake pad, no root; the box was
otherwise idle (load ~1.5/16 threads, Kodi at home screen, no game).

## Sequencing note the runbook did not anticipate

The E-runbook predates the installed 72-rule. With the rule armed, udev
gives the FAKE pad to root:couchd-input (proof the rule works, incidentally)
and the no-root ladder cannot open it. Resolution used: disarm via
`sudo -n couchd-input-release` (the sanctioned lever, no password), run the
ladder in the runbook's expected state, re-arm after. Fold this into the
runbook if E1 ever re-runs post-install.

## E0 — PASS (one run, gate for all three E1 runs)

- E0.1: 3 input nodes + power_supply present, hid_playstation bound.
- E0.2: `13b 8a`, BTN_MODE present: True.
- E0.3: tap pair 0.121s apart; hold 1.201s between edges; BTN_SOUTH travels.
- E0.4 equivalent: corpus 19 files before and after every run.

## E1 — PASS, k=3 (three separate rig starts)

Every run: supervisor connected to the LIVE couchd (a first — the runbook's
"no rung drives both together" caveat is now partially retired: connection,
events flowing, no drops; the acting side is still untested).

| gate | run 1 | run 2 | run 3 |
|---|---|---|---|
| E1.2 grab + vpad `11b 8a` + FF_RUMBLE/FF_GAIN | ok | ok | ok |
| E1.3 second grab EBUSY | ok | ok | ok |
| E1.4 face-button table (square->BTN_NORTH etc.) | exact | exact | exact |
| E1.5 tap = one chord ~80ms AFTER release; hold = ZERO BTN_MODE | ok | ok | ok |
| E1.6 FF `created 16 refused 4` / update / erase / re-create 16 | ok, 0.2ms | ok, 0.3ms | ok, 0.2ms |
| E1.7 p50 / p99 ms | 0.017 / 0.455 | 0.019 / 0.035 | 0.018 / 0.041-0.06 |
| E1.8 Kodi flavour flatpak, map RESOLVES (app-image root) | ok | ok | ok |
| E1.9 reconnect-in-window | ok (see note) | **"re-upload: 2 ok, 0 failed"** + holder play OK | same, green |
| teardown: units restored, corpus 19, no leftover vpad | ok | ok | ok |

- E1.9 expiry case (once per k=3): run 1 — window expired at exactly +30s,
  "destroying the virtual pad so Steam can free the player slot", fresh
  vpad created on return. PASS.
- E1.9 run-1 note: first reconnect showed `reupload ok=0` because the
  seeding script had exited - the KERNEL erases a client's FF effects when
  its fd closes, so a transient uploader cannot test re-upload. Runs 2-3
  used an fd-holding client (upload, hold fd across the drop, play after):
  both gave `2 ok, 0 failed` and a working post-reattach rumble. Not an
  inputproc bug; worth a runbook footnote.
- E1.7 anomaly, recorded per the "one run is weather" rule: run 3's first
  tick window showed p99=2060ms (p50 normal at 0.021). Cause almost
  certainly the test harness itself - each fake-pad call is a fresh python,
  and that window overlapped a ~400-process fork storm plus three reader
  scripts starting. Two immediately following windows: 0.06 / 0.041ms.
  If flag day ever shows multi-second p99 under REAL load, do not wave it
  off with this note - this box was self-inflicting a fork storm.

## State after the session

- Rule status: disarmed for the ladder via couchd-input-release; re-arm
  (rig-scoped mv + reload) handed to Donnie immediately after this file was
  written - see todo for whether it landed. Unit still disabled. Nothing
  routed.
- Next per the plan: fix pad-connect-daemon + tv-waker any-`js*` checks,
  then E2 (Steam adopts the vpad; daytime; back up
  `~/.steam/debian-installation/config` first), then flag day
  (INSTALL.md step 9 + step 10 evidence proof).
