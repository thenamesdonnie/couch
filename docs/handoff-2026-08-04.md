# Handoff — couchd build session, 4 Aug 2026

## MORNING SUMMARY (5 Aug, overnight run + the 05:19 crash)

Everything below happened while you slept. Six commits, all stages green,
nothing acts: owns.conf is empty, couchd is still pure shadow.

**Your box hard-froze at 05:19, and it was not my work.** Lights on, no
ssh, no video; you power-cycled it at 08:05. Zero kernel traces, box was
idle, no GPU errors. It is the first-ever freeze of this box and it came
27 hours after the borrowed 32GB went in. The RAM config (not the sticks)
is the prime suspect: same sticks, different board, different memory
training. Your 5 minutes: run
`sudo dmidecode -t 17 | grep -E "Speed|Part|Size|Locator"` and if
"Configured Memory Speed" shows the boosted profile, drop BIOS to stock
speed for a few days as the cheap stability test. Also unexplained: a
16-minute graphical-session cycle at 04:41 that was not the agents, not
an update, not lightdm. Everything recovered green on reboot by itself.

**What is FLIP-READY (build done, adversarially reviewed, ships OFF):**
- Acting executor + COUCHD_OWNS in couchd, one grammar both stacks read.
  Ownership is a lease on couchd's heartbeat: plain
  `systemctl --user stop couchd` self-heals back to legacy within 30s.
- Legacy yielding: watcher/guard/game-launch skip what couchd owns and
  log their would-do instead (they become the shadow). Provably no-op
  while owns.conf is empty; the reboot live-validated that.
- Write-through of /tmp flags, byte-exact; transient-unit spawning so
  acted commands escape couchd's sandbox; failure backoff; C17 pre-checks.

**Fixed overnight:** the 03:00 startup hang (stale fifo blocking open),
x11 observer silence (events were being swallowed, now wired end to end),
gesture decisions ride the button edge instead of the 5s tick, 9 of 10
T5 model catch-ups, 2 post-reboot bugs (absent /tmp log, born-late log
tail), and 3 real couchd bugs the Evening-1 replay caught, including two
that would have gated flips (unconditional guide press that would have
OPENED the Steam menu on half the handoffs; SIGTERM at a stale pid).
Replay verdict: catch-ups closed 38 divergences, matched decisions 93 to
138, nothing regressed. Full analysis: docs/replay-2026-08-05.md.
Suites: couchd 410, tools 167, gitleaks clean.

**Your daytime 5-minutes (in order):**
1. The dmidecode check above (2 min, before anything else).
2. `systemctl --user restart pad-home` before ANY flip: the running
   watcher predates the yield edits. This is flip checklist step 0.
   (After the 08:05 reboot it already runs the new code, so this is
   already done unless you restore something.)
3. Fault-rig session with me present via phone runbook:
   docs/fault-rig-runbook.md (needs a live game, ~20 min). The replay
   showed 38 of 94 transition edges never exercised; this is the
   highest-value gap.
4. First flip when you are ready: name ONE responsibility in
   couchd/owns.conf (gestures is the argued first), 5-min couch
   acceptance, rollback is emptying the file or stopping couchd.

**TONIGHT (Evening 2) - the numbered couch list, phone-readable:**
1. Get home, TV off, connect the pad. TV should wake in ~6-8s (this
   proves today's tv-waker fix). If Kodi ignores the pad for more than
   ~10s, tell Claude, but the new auto-repair should kick in by itself.
2. Play normally for a bit. Everything PS-button is now couchd acting
   (flipped at 13:01, your 13:56 mini-test passed: 8 actions, 0 fails).
   You do not need to do anything special. Just say if anything feels
   off or slow, with a rough time.
3. Somewhere in the evening, do one deliberate round: launch a game
   from a tile, hold PS to suspend, tap PS to resume, double-tap for
   the switcher, switch to TV and back.
4. If you have 20 min and Claude is around: the fault-rig session
   (docs/fault-rig-runbook.md), it needs a live game and you present.
5. That's it. The corpus records everything; tomorrow's differ run is
   the evidence. Two unexplained wrongs = flip back to shadow (one
   line, or just: systemctl --user stop couchd).

**Three rulings only you can make (no code blocked on them):**
- Settle window: should a press swallowed during the 1.2s settle seed
  the double-tap window? (The one T5 catch-up not modeled.)
- Phone suspend path: the phone's direct suspend/quit buttons act in
  full even when couchd owns gestures/transitions. Second arbiter or
  user-intent exception?
- Differ and `launch`: legacy launches stay context-only; should
  couchd's model_only BP pre-steps be diffed? (Replay finding 5.)

---

Everything that happened while you were at work, what to test from the
couch, and the two decisions only you can make. Written to be read on a
phone. Details live in: docs/couchd-stage1-design.md,
couchd-stage2-design.md, couchd-stage3-design.md, couchd/stage2/INSTALL.md.

## 0. BEFORE you use the pad tonight — one command

    systemctl --user stop app-dev.lizardbyte.app.Sunshine.service

Sunshine (game streaming, no client connected) got pulled back up at
21:28 as a side effect of enabling couchd, and its virtual mouse
registers as /dev/input/js0. Both pad-connect-daemon (the outbound dial
that is the ONLY way your pad connects, because of the kernel L2CAP bug)
and tv-waker treat "any js node" as "pad already there" — so with js0
present your pad may refuse to connect and the TV won't auto-wake. I was
permission-blocked from stopping it myself. Root fix is in section 5.

## 1. What's now running on the box (all reversible, TV-safe)

- **couchd.service** (user unit): the stage-1 SHADOW daemon. Observes
  everything (pad, flags, pids, Kodi, X, Steam logs), runs the explicit
  state machine, logs what it WOULD do. It acts on NOTHING — there is
  literally no acting executor in the codebase. Kill switch (also on the
  audit cheat sheet): `systemctl --user stop couchd pad-record`.
- **pad-record.service**: evdev recorder (fixed tonight: mtime-stamped
  markers, inline gzip, 14-day retention). Records automatically when
  the pad appears.
- **Old stack, instrumented**: watcher/game-launch/guard now also write
  machine-readable decisions to /tmp/legacy-intents.jsonl (log-only
  additions, behavior untouched; watcher restarted and rig-verified).
- **Phone**: Screen tab now has a **couchd card** at the bottom (regions,
  observer health, last would-do). couch server restarted on env-based
  config (secrets now live in .env only).
- **Git**: ~/couch is a repo now, 15+ commits, history born clean
  (gitleaks-verified). Local-only, no remote.

## 2. Your couch checklist (same as before, now doubly shadowed)

In Elden Ring, nothing to set up — recorder + shadow run themselves:
1. Hold PS: game truly freezes (CPU drops; first time this ever worked).
2. From Kodi home, tap the game tile: resumes.
3. In-game tap PS: Steam menu opens; tap again: closes.
4. Try to break it: spam presses, ~0.8s ambiguous presses, presses
   during transitions.
5. Phone panic button: Screen tab -> switcher -> Kodi mid-game.
6. TV off, start a video / launch a game from the phone: TV wakes ~6-8s
   (AFTER doing section 0, else the wake may not fire).

If anything sticks >10s: note the time; logs are /tmp/pad-home.log,
/tmp/game-launch.log, /tmp/steam-input-guard.log, /tmp/couchd.log,
/tmp/pad-record.log. The morning after any evening:
`~/couch/tools/shadow-diff` prints the couchd-vs-legacy report.

## 3. What got built (the short version)

- **Stage 1 design note**: research-rooted (3 agents: parallel-run/shadow
  practice, reconciler/statechart architecture, console-daemon prior
  art incl. on-box Steam observation finds like gameprocess_log.txt as
  Steam's own PID ledger), then torn apart by 2 adversarial reviewers
  (their biggest catch: the old stack ALREADY logs its decisions, so the
  planned inference engine died and ~4k LOC became ~1k). All rulings in
  the note (R1-R8).
- **couchd** (2.2k lines Python + 260-line X adapter): 7 observers,
  orthogonal state regions with `unknown` everywhere, pure
  reconcile(observed)->intents, invariants incl. owned-resources leak
  checks, event-sourced JSONL corpus, status.json for the phone,
  systemd notify+watchdog, 30 tests + Hypothesis property machine.
  Found real things already: the guard's stale pidfile leak, and it
  tracked synthetic PS taps through the gesture region on kernel time.
- **shadow-diff** (v2, 22 tests): the morning report — three divergence
  sections, T1-T6 triage with pre-declared whitelists, happens-before
  assertions, evening-validity gate. First real run: VALID, 0 diffs.
- **Stage 2 designed + built synthetic-only**: custom input ownership
  (InputPlumber evaluated seriously and declined WITH receipts: root
  daemon, crash-bricks-pad bug #582, 20-month rumble bug #224, no
  kernel timestamps). Adversarial review found 8 blockers (incl. the
  no-root claim being unimplementable as first written and 0640 silently
  killing rumble) — all ruled into SR1-SR9. Built: gesture module shared
  with stage 1, inputproc (grab + virtual X360 exact-vpad-clone + full
  FF contract incl. the erase-stall and face-button-swap traps),
  fake-pad uhid rig (E0 PASSED tonight on real kernel binding), 96
  tests, and couchd/stage2/ = your one sudo session (INSTALL.md, ~10
  min, includes rollback rehearsal).
- **Stage 3 (gamescope): PARKED** with a written decision record — on a
  1080p60 SDR TV there is nothing to win and real costs. Revisits when
  the TV upgrades. ~/gamescope-deps.sh is wrong as staged; corrections
  in the decision record.

## 4. Legacy bugs found tonight (NOT fixed — your call)

Pre-declared as expected diffs (T3) so the shadow doesn't hide them:
- **(a)** Pure Big Picture PS-holds never write /tmp/game-suspended, so
  ~10s later reconcile takes the pad off Kodi — pad routed to nobody.
- **(b)** Games launched FROM Big Picture record appid as the literal
  string "bigpicture"; that game's Kodi tile then CLOSES it instead of
  resuming. (PS tap and phone resume still work.)
- **(c)** pad-connect-daemon + tv-waker "any js*" checks (section 0's
  root cause) — also means ANY virtual joystick (vpad rigs, Sunshine)
  silently breaks pad reconnect + TV wake.
- **(d)** guard's pidfile isn't removed on normal exit (leak flagged by
  couchd's invariant every tick).
- Three `steam_app_*` class predicates are fragile identity assumptions
  (guard:272, screen.js:271+291) — dissolve naturally as couchd absorbs
  those responsibilities; fix earlier only if gamescope ever un-parks.

Say the word and I'll fix any of these in the old stack (small, but
they're behavioral changes to live console plumbing, so your call).

## 5. Your sudo session (~10 min, whenever suits — NOT needed tonight)

Everything is staged in couchd/stage2/ with exact commands in
INSTALL.md: create couchd-input group + system user, install the
(initially .off) udev rule, the NOPASSWD rollback script
(couchd-input-release), inputproc.service, and REHEARSE the rollback.
Plus two small root fixes worth doing in the same sitting: make
pad-connect-daemon and tv-waker test for the DualSense's own device
instead of any js* (fixes 4c properly).

## 6. Where the cutover stands (the honest picture)

Stage 1 shadow needs **evenings**: 10-15 clean real sessions (shared
across responsibilities, transition coverage is the unit) with zero
gating diffs, plus injected-fault runs for the repair paths that never
fire organically. Every evening you just use the TV normally counts.
Then responsibilities flip one at a time behind COUCHD_OWNS, daytime,
with the old stack still installed. Stage 2's flag day (real pad
ownership) is after that, you present, checklist in the stage-2 note.

Deferred to a daytime window (they launch games / turn the TV on):
psfuzz + chaos identity runs with couchd live, E2 (Steam sees only the
virtual pad; needs Steam config backup first), STEAM_GAMES_RUNNING
atom re-test with a game running.

## 7. E0 + E1-functional: PASSED (fake pad, 4 Aug late night)

E0: the uhid fake DualSense binds hid_playstation for real (3 nodes,
power_supply, BTN_MODE on kernel timestamps). E1, three consecutive
clean runs, every SR9 gate green: forwarding p99 0.065-0.17ms against a
10ms budget (~60x margin); the full force-feedback contract live
(20-upload exhaustion, update-reuses-slot, the erase path that would
otherwise stall a closing game 30s/effect, gain passthrough); PS tap
re-injected at 80.3ms chord pacing while a 1.5s hold is completely
swallowed; exclusive grab EBUSY-confirmed; persistence machine (BT loss
-> keys-up -> reconnect -> FF re-upload; give-up -> node destroyed)
exercised on the wire; the Kodi X360 buttonmap resolves for the virtual
pad's exact shape; face-button transposition (square=BTN_WEST etc.)
confirmed on hardware, so games won't get X/Y swapped. Every run tore
down clean (services restored, corpus untouched, zero leaked devices).

What E1 could NOT prove (all on your flag-day list): the ownership
mechanism itself (udev rule + group are staged, not installed — your
sudo session); Steam adopting the virtual pad (E2, daytime, config
backup first); real-Bluetooth drop behavior on the patched bluetooth.ko;
Elden Ring rumble via X360 emulation (unverified anywhere upstream).
Plus one caution added to flag day: a wedged inputproc holding the grab
= dead pad; its unit has TimeoutStopSec=10 + KillMode=mixed, but give it
a deliberate wedge-test before trusting it.

## 8. TWO NEW FEATURES you asked for — both LIVE and verified

**Double-tap PS -> TV switcher.** Double-tap (two taps inside 0.35s)
opens a Kodi dialog you scroll with the stick: the running/paused game,
Big Picture, Desktop, Cancel. It runs the same state-aware code the
phone switcher uses, so picking Kodi mid-game does a proper suspend, and
picking a paused game resumes it. Verified on the box: the dialog
renders, a 0.4s gap correctly does NOT trigger it, and your existing
tap/hold behavior is bit-for-bit unchanged.
- Mid-game caveat (stage 1 only): Steam still hears both raw presses over
  hidraw, so its menu may flash briefly before the guard tidies up. On
  the Kodi home screen it's clean now. Stage 2 makes it exact by
  swallowing the presses.
- While a game is PAUSED, a tap resumes it (that's the recovery path), so
  double-tap can't open the switcher in that state — use the phone.

**Gesture keybinds, in Kodi's own settings.** Kodi > Add-ons > Program
add-ons > Couch Switcher > Configure. Bind each of tap / double-tap /
hold / hold-release / long-hold to any of: nothing, back to Kodi
(suspend), switcher, Steam menu, power menu, quit game, TV toggle,
Desktop. Timings (hold 0.9s, double-tap 0.35s, long-hold 3.0s) are
sliders under Advanced — calibrate them to your thumb; the recordings
will tell us what your real numbers are.
- Changes apply INSTANTLY, no restart (both the watcher and couchd's
  shadow read the same file, so the shadow diff stays honest).
- Safety rail verified live: unbind the last gesture that gets you back
  to Kodi and the whole config is rejected with a warning, hold forced
  back. You cannot strand yourself from this menu.
- Hold beats long-hold: long-hold only fires if hold is set to Nothing.
- Verified live tonight: rebound double-tap to the power menu, felt it
  change with no restart, tripped the safety rail on purpose, restored
  defaults.

(If either misbehaves: both are additive. Disable the addon in Kodi, and
`systemctl --user restart pad-home` after reverting the watcher —
everything is in git now, so `git diff` shows exactly what changed.)

**Paused-game freeze-frames (third feature, added late).** PS-hold (or
phone-suspend) out of a game now captures its last rendered frame: the
phone switcher row shows it as a thumb, and the game's Kodi tile + fanart
show it while paused, reverting to normal art on resume/quit. Verified
against a live Proton window; the one untested combination is the full
real sequence, so your test is simply: pause Elden Ring, look at the
tile, resume. (PiP and animated Kodi<->game transitions are deliberately
NOT hacked in — they're stage-4-native; your ask is recorded as a revisit
trigger in the stage-3/4 notes.)

**Integration note (5 Aug, ~00:20):** all of the above is LIVE — the
accidental KCD2 launch was quit cleanly, web rebuilt, watcher/couch/
couchd restarted and re-verified. The recordings corpus was purged of
synthetic rig files; your first real pad session becomes recording #1.

---

# EVENING 1 SCRIPT — the directed gate pass (5 Aug, Claude watching live)

Read each step, do it, glance at what you SHOULD see. If a step looks
wrong, just tell Claude the step number — don't debug, keep moving unless
the TV is actually stuck. Claude runs the differ between parts and may ask
you to redo a step after a fix; that's the plan working, not failing.

## Part A — arrival (5 min)
1. TV off, pad off. Press PS to connect the pad.
   SEE: pad connects, TV wakes in ~6-8s onto Kodi. (First test of the
   pad path with Sunshine stopped.)
2. On Kodi home: single-tap PS. SEE: nothing dramatic (tap is unbound).
3. Double-tap PS. SEE: the switcher dialog. Scroll with the stick,
   pick Cancel.

## Part B — gestures on a fast game (Hollow Knight, ~20 min)
4. Launch Hollow Knight from the Kodi Games row tile.
5. In-game: tap PS → Steam menu opens. Tap again → closes.
6. HOLD PS (~1s). SEE: back on Kodi home; the game's tile now shows a
   FREEZE-FRAME of where you left it, marked paused.
7. Phone → Screen tab: the switcher row shows the same thumbnail.
8. Tap the game tile → resumes exactly where you were; tile art reverts.
9. Hold PS again → home. Now TAP PS. SEE: resumes (tap = recovery path).
10. Hold PS → home. Double-tap → pick the game in the dialog → resumes.
11. In-game: spam PS taps (5, fast). SEE: Steam menu may flap; within a
    couple of seconds everything settles and the game still has the pad.
12. Three ambiguous ~0.8s presses. SEE: nothing sticks either way.
13. Press PS mid-resume (during the transition). SEE: converges in ~2s.
14. Mid-game, phone panic: Screen tab → pick Kodi. SEE: proper suspend,
    freeze-frame updates to the new spot.
15. Resume from the phone.
16. Double-tap → pick Desktop. SEE: desktop, game suspended. Come back
    via phone → pick the game.
17. Mid-game: turn the pad OFF, wait 10s, turn it back on. SEE: pad
    reconnects and you're still in the game.
18. Quit properly: hold PS → home → hold PS → power menu → quit game.
    SEE: clean exit, tile art back to normal.

## Part C — the other launch paths (~10 min)
19. Open Big Picture, launch a game from INSIDE it. KNOWN BUG (b): its
    Kodi tile will misbehave while paused — note it, use PS tap or phone
    to resume, quit via power menu.
20. Launch a game from the PHONE (Games tab). Play 30s, quit.
21. shadPS4 title: launch, hold-PS out, resume, quit.
22. Pure Big Picture, NO game: hold PS. KNOWN BUG (a): within ~10s the
    pad may go to nobody. Recover: phone → Screen → Kodi. Tell Claude
    the exact time.

## Part D — media + keybinds (~10 min)
23. Start a film from the phone. SEE: lights dim; pause → brighten;
    stop → restore.
24. Kodi → Add-ons → Program add-ons → Couch Switcher → Configure.
    Rebind double-tap to Power menu. Double-tap: SEE power menu, no
    restart needed. Rebind back. Double-tap: switcher again.
25. Advanced: set hold to 1.2s, feel the slower hold, set back to 0.9.

## Part E — fault injections (Claude drives, you watch, ~15 min)
26-30. With a game session up, Claude stages the five repair scenarios
    (orphaned session, stale flag, lost thaw, joystick drift,
    frozen-game-visible) from SSH. Your only job: confirm the TV
    recovers by itself each time within ~15s, and say if anything you
    see disagrees with what Claude says should be happening.

## Part F — free play
31. Play whatever you actually want for 30-60 min. This is data too —
    the organic tail the script can't fake. (KCD2 is sitting at its
    EULA screen from earlier, if you fancy it.)
